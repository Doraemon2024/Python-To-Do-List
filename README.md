# Python-To-Do-List
Add Tasks: Users can input a new task name, which is then added to a list or similar data structure.
View Tasks: The program displays all current and pending tasks, usually in a numbered list format.
Delete/Remove Tasks: The application allows users to remove completed or unwanted tasks from the list.
Exit:The application allows users to exit from program.
